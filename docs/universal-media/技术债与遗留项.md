# Universal Media 技术债与遗留待修项

> 跨阶段技术债登记。Phase 1 验收通过，以下为收尾遗留，安排在 Phase 2 期间清理。
> 来源：Phase 1 代码复检（2026-06-21）。

## 待修清单

| # | 级别 | 问题 | 位置 | 修复要点 | 状态 |
|---|---|---|---|---|---|
| TD-1 | **P2 安全** | 代理跟随 3xx 重定向不重校验 → SSRF 绕过 | `internal/handlers/media/source_play.go` | `CheckRedirect` 每跳重跑 `ValidateOutboundURL` + 限次 | ✅ 已修(T10, ef3d5f1) |
| TD-2 | **P2 维护** | 单文件 1419 行破 800 红线 | `internal/repository/source_repository.go` | 按域拆多文件 | ✅ 已修(T10, fe883b3，拆为 source_{types,config,provider,item,play,view,resolve,scan}_repository.go) |
| TD-3 | P3 安全 | DNS rebinding（校验 IP 与拨号 IP 可能不同） | `internal/source/ssrf.go` + 拨号层 | 自定义 `DialContext` 校验实连 IP | 待修（T10 可选项，未做） |
| TD-4 | P3 稳定 | 代理无 `ResponseHeaderTimeout`，上游卡住挂 goroutine | `internal/handlers/media/source_play.go` | 设 `Transport.ResponseHeaderTimeout` | ✅ 已修(T10，随 TD-1 一起) |
| TD-5 | P3 打磨 | admin DTO JSONB 被 base64 序列化 | `internal/repository/source_*_repository.go` | 改 `json.RawMessage` 内联 | ✅ 已修(T10，实测 RawConfig 已内联) |
| TD-6 | P3 一致性 | 聚合搜索 per-provider 超时(默认 8s) < 单站 Search 路径(共享 client 15s)：慢但活着的站在"测试搜索"能成、在聚合里被 8s 砍掉 | `internal/source/federated_search.go` `federatedProviderTimeout` | 对齐到与单站一致量级(12~15s 或取 provider.TimeoutMS 兜底)，整体预算随之调整 | ✅ 已修(37e70e5，按运行时默认取值并对聚合默认封顶 15s) |
| TD-7 | P3 打磨 | 个别站(索尼)搜索响应"格式无法识别"；Emby 搜索同标题跨 provider 可能重复 | `internal/source/cms_provider.go` detectCMSResponseFormat / Emby 搜索合并 | 空响应/异常前缀归一为不可用；Emby 搜索出口可选做跨 provider 去重 | ✅ 已修(92b124e/1a7c7f9，CMS 非标准响应归一不可用；Emby 外部源 hint 按归一标题+年份去重) |
| TD-8 | P3 运维 | CSP(JVM)/JS(Node) sidecar 运行期依赖 PATH 有 java/node；裸机启动 FYMS 前需手设 PATH，漏了则 runtime_unavailable | sidecar 启动逻辑 | 支持 `FYMS_JAVA_BIN`/`FYMS_NODE_BIN` 配置项显式指定可执行文件路径，免依赖 PATH（Docker 已内置 JRE/Node，仅裸机受影响） | 待修（不阻断） |

## 说明

- TD-1/2/4/5 已在 Phase 2 T10 清理；TD-3 为可选项未做。
- TD-6/TD-7 为 Phase 2 T13/T14 复检新发现，已补齐收口；正常 2s 上游不受 TD-6 调整影响，异常响应与跨源重复 hint 已归一处理。
- Phase 2 复检结论：T10~T15 功能完整、核心不变量(不写 items / UUID 命名空间 / 存储分离)持续守住。
